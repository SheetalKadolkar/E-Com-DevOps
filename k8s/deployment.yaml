apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-deploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ecommerce
  template:
    metadata:
      labels:
        app: ecommerce
    spec:
      containers:
      - name: ecommerce
        image: sheetalkadolkar/e-com:latest
        ports:
        - containerPort: 5000
